
all:
	neato -Tpdf 3event_reverse_po.dot > 3event_reverse_po.pdf
	neato -Tpdf 4event_change_address.dot > 4event_change_address.pdf
	neato -Tpdf 4event_reduce_sync.dot > 4event_reduce_sync.pdf

clean:
	rm -rf *~ *.pdf
